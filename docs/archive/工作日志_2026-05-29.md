# 工作日志 — 2026年5月29日

## 今日完成

### 1. FastAPI 后端 SQLite 适配
- 将 PostgreSQL 依赖切换为 SQLite 开发模式
- 修改的文件：database.py / models.py / search_service.py / main.py / requirements.txt
- 14 项测试全部通过
- FastAPI 服务运行在 localhost:8000，含 /health 和 /docs 端点

### 2. COS STS 临时凭证服务
- 在 backend/routers/cos_sts.py 创建 /cos/credentials 端点
- 使用主账号 COS 密钥签发限权临时凭证，策略限制到 ref/* 和 gen/* 路径
- 安装 tencentcloud-sdk-python-sts
- 挂载到 main.py，接口验证通过

### 3. OneDrive 图片清理
- 账号：Aminu0918@outlook.com（5GB 容量，之前已满）
- 通过 Microsoft Graph API 设备代码流完成 OAuth 授权
- 使用 aiohttp 异步下载 2827 张图片的缩略图
- OpenCV Haar Cascade 人脸检测分类
- 保留 33 张含人物/模特照片
- 删除 2436 张无人脸图片（约 4.3 GB）
- 验证：已用空间从 5.0GB 降至 0.7GB

### 4. OneDrive 清理技能封装
- 位置：C:\Users\Aerc\.codex\skills\onedrive-photo-cleanup
- 包含 4 个脚本：od_auth.py / od_scan.py / od_classify.py / od_delete.py
- 技能验证通过，下次可直接调用 "Onedrive Photo Cleanup" 技能

### 5. iPhone 选择性上传方案调研
- 关闭 OneDrive 相机备份，改为手动选照片上传
- 推荐使用 iPhone 快捷指令 + 文件 App 中转
- 已创建快捷指令方案说明

## 运行中服务
- TypeScript API (mock模式)：localhost:3000
- FastAPI 后端 (SQLite)：localhost:8000
- COS STS 接口：localhost:8000/cos/credentials
