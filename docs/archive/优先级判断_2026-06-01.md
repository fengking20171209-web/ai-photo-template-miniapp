# 当前优先级判断 — 2026年6月1日

## 状态快照

| 维度 | 状态 |
|------|------|
| 工程底座 | ✅ 生产可用（P0/P1 清零） |
| 模板内容层 | ✅ 42 模板 / 10 分类 / 全量加载正常 |
| 生图能力层 | ⏳ mock 通，真 API 未验证 |
| 前端体验层 | ⏳ 有基础框架，未串闭环 |
| 存储与分发 | ⏳ COS STS 已通，防盗链/CDN 未配 |

---

## 优先级判断表

| 方向 | 是否现在做 | 理由 |
|------|:----------:|------|
| **真 API 接入测试** | **✅ 是** | 内容层已闭环，能力层是最大风险点。HTTP 适配层全是假设，从未被真实接口验证。尽早暴露参数格式、响应结构、超时策略问题，否则前端闭环等于在沙上盖楼。 |
| **前端闭环** | ⏳ 紧接 API 后 | 上传→选模板→生成→看结果，依赖 API 返回的数据结构（URL 字段名、任务状态格式）。API 验证完再做，设计才稳。 |
| **模板继续扩充** | ❌ 否 | 42 个模板已覆盖 10 个分类，继续扩只是量变。等 API 跑通后再根据真实出图效果筛选和增补。 |
| **COS 防盗链 + CDN** | ⏳ 前端闭环后 | 资源保护是必要项，但当前没有公开流量，不阻塞开发。前端闭环完成后配。 |
| **P2 工程债务** | ❌ 否 | Alembic、requirements 锁定、路径硬编码等，不影响产品闭环。挂到 backlog，商业化前扫。 |
| **批量上传参考图到 COS** | ❌ 否 | `local_cache/inbox/` 没有阻塞性积压，自动化脚本已存在。需要时手动触发即可。 |
| **抖音小程序** | ❌ 否 | Phase 3，需要 API 稳定、支付对接、审核周期。当前完全不急。 |
| **数据库切 PostgreSQL** | ❌ 否 | 生产部署项，当前 SQLite 完全支撑开发和本地测试。 |

---

## 下一步唯一动作

**把 `.env` 里的 `AI_IMAGE_PROVIDER` 从 `mock` 切到 `http`，选 1 个简单模板（`product_perfume_luxury`，无人物、无 face_lock），跑一轮真实调用，确认：**

1. 请求体 JSON 被目标 API 正确接收
2. 响应中能提取到图片 URL
3. `output/image_task.json` 正确写入结果
4. 超时、异常、降级策略在真实网络环境下有效

这是当前唯一的高优先级事项。其余全部让路。
