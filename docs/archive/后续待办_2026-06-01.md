# 后续待办 — 2026年6月1日

> 工程基础设施五轮修复已完成（13 个文件），底座达到生产可用状态。

## 高优先级

### 产品方向
- [x] **模板扩充**：从 `references/awesome-gpt-image-2-API-and-Prompts/cases/` 提炼新模板
  - [x] 电商产品类（香水、护肤品）→ `product_perfume_luxury`、`product_skincare_chamomile` → 归入 `"产品海报"`
  - [x] 人像写真类（便利店霓虹、日式温泉、卧室镜拍）→ `portrait_convenience_neon`、`portrait_onsen_ryokan`、`portrait_mirror_bedroom` → 新建 `"人像写真"` 分类
- [x] **扩展模板分类硬编码**：`src/types/template.ts` 和 `src/templates/templateSchema.ts` 从 6 个扩展到 10 个分类
- [x] **修复数据质量**：`concept_neo-chinese-bodycon.json` 重写为标准格式，`template_id` 和 `category` 修正
- [x] **批量迁移旧模板**：`dudou_d01~05` + `siuf2026_01~25` 共 30 个模板的十层字段映射到标准八字段
- [ ] **真 GPT-Image-2 / SenseNova 生图 API 接入测试**：Node.js 侧已支持 `sensetime` → `http` 映射；Python 后端 SDK 认证已修复；**卡壳：SenseNova API Key 无效（401 Forbidden），需用户修复凭证或换供应商**

### 前端
- [x] 前端页面闭环（mock 模式）：模板浏览 → 选模板 → 一键生成 → 查看结果
- [ ] COS 防盗链配置：Referer 白名单保护 COS 资源

## 中优先级

- [ ] 批量上传更多参考图到 COS（`local_cache/inbox/` → `batch_import_refs.py`）
- [ ] TypeScript 模板引擎完善（更多模板、更丰富的 prompt_blocks）
- [ ] iPhone 快捷指令创建并测试（选照片 → 存本地 → 上传 OneDrive）
- [ ] `public/app.js` COS v4 签名实现验证（当前为简化版，需测试是否兼容腾讯云）

## 低优先级（工程债务 / P2）

- [ ] Alembic 数据库迁移（替代 `Base.metadata.create_all`）
- [ ] `requirements.txt` 版本锁定（生成 `requirements.lock` 或 `uv pip compile`）
- [ ] `scripts/*.py` 硬编码 Windows 绝对路径去硬编码化
- [ ] OneDrive 清理技能后续优化（重试机制、进度通知）
- [ ] 百度网盘同步自动化（PowerShell 脚本定时触发）
- [ ] FastAPI 后端生产部署（切换回 PostgreSQL + 密钥管理）
- [ ] COS 图片 CDN 加速配置

## 已完成（本轮）

- [x] 工程基础设施五轮修复（13 个文件，P0+P1 全部清除）
- [x] `.env.example` 补全（20+ 变量 + 注释）
- [x] 项目只读审计（22 项问题识别 + 摸底报告）
- [x] 分类硬编码修复（`template.ts` / `templateSchema.ts` → 10 个分类）
- [x] `concept_neo-chinese-bodycon.json` 数据修复（重写标准格式 + 修正 category）
- [x] 新建 5 个模板（3 个人像写真 + 2 个产品海报）
- [x] 30 个旧模板批量迁移（十层 → 八层字段映射）
- [x] 前端闭环（`app.js` + `index.html`）：选模板 → 一键生成 → 结果展示 → prompt 预览
- [x] Python 后端 SenseNova SDK 认证修复（`access_key_id` → `secret_access_key`）
- [x] Node.js `appConfig.ts` 支持 `sensetime` → `http` 映射
