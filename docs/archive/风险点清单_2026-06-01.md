# 风险点清单 — 2026年6月1日

## 🔴 高风险

| # | 风险点 | 影响 | 当前状态 | 缓解措施 |
|:--:|--------|:----:|:--------:|----------|
| 1 | **真 API 从未验证** | 高 | 阻塞 | HTTP 适配层全是假设，未接真实接口。mock 跑通≠真实可用。凭证就绪后必须立刻做端到端验证。 |
| 2 | **模板 prompt 未经生图验证** | 高 | 潜在 | 48 个模板的 prompt 全是"纸面推导"，未经过真实生图效果检验。可能有些模板实际出图效果差。 |
| 3 | **旧模板自动化迁移质量** | 中 | 潜在 | 30 个旧模板（dudou/siuf）用脚本从十层字段映射到八层字段，可能存在语义丢失或拼接不自然。 |

## 🟡 中风险

| # | 风险点 | 影响 | 当前状态 | 缓解措施 |
|:--:|--------|:----:|:--------:|----------|
| 4 | **前端 API 路径不一致** | 中 | 部分修复 | `/api/templates` 已确认错误（应为 `/templates`），但画廊 `/images/recent` 仍依赖 Python 后端（8000 端口），前端 `API_BASE = ''` 无法同时对接两个后端。 |
| 5 | **分类硬编码维护成本** | 低 | 已暴露 | 每次新增分类需改 `template.ts` + `templateSchema.ts` 两个文件。未来应考虑从模板目录动态读取分类列表。 |
| 6 | **SenseNova 供应商锁定** | 中 | 潜在 | 当前 `.env` 配置偏向 SenseNova，但凭证无效。若最终换供应商（GPT-Image-2 / EvoLinkAI），Node.js HTTP 适配层的请求体格式可能需要调整。 |
| 7 | **COS v4 签名未验证** | 中 | 潜在 | `public/app.js` 中的 `buildCosAuth` 是简化版实现，未在真实腾讯云环境测试过兼容性。 |

## 🟢 低风险

| # | 风险点 | 影响 | 当前状态 | 缓解措施 |
|:--:|--------|:----:|:--------:|----------|
| 8 | **前端 `navigator.clipboard` 兼容性** | 低 | 潜在 | 部分浏览器/非 HTTPS 环境下可能不可用。已有降级方案（`document.execCommand('copy')`）。 |
| 9 | **模板 ID 命名不一致** | 低 | 已修复 | `concept_neo-chinese-bodycon` 曾被错标为 `siuf2026_neo`。其他模板是否也有类似问题？建议做一次全量 template_id vs 文件名一致性检查。 |
| 10 | **Python 后端与 Node.js 后端并存** | 低 | 现状 | 两个后端（3000/8000）并存，长期维护成本高。未来需明确主后端是 Node.js 还是 Python。 |

---

## 建议的下一步风险处置

1. **立刻修复前端 API 路径**：`public/app.js` 中 `/api/templates` → `/templates`（1 个文件）
2. **模板 ID 一致性检查**：写脚本校验所有 `templates/*.json` 的 `template_id` 是否与文件名一致
3. **真 API 验证**：一旦凭证就绪，优先用 `product_perfume_luxury`（无人物、最简单）做端到端测试
