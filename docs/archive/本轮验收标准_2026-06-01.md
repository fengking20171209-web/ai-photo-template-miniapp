# 本轮验收标准 — 2026年6月1日

## 一、工程底座验收

| 验收项 | 命令 | 通过标准 |
|--------|------|----------|
| TypeScript 类型检查 | `npm run check` | 零错误，零警告 |
| 模板加载验证 | `npm run list` | 48 个模板全部加载，无 `unsupported category` 报错 |
| 指定模板生成测试 | `npm run generate <template_id>` | `output/prompt.txt` 正常输出，`output/task.json` 正常写入 |
| 前端服务启动 | `npm run api` | `localhost:3000` 可访问，`/health` 返回 `provider: mock` |

## 二、数据质量验收

| 验收项 | 验证方式 | 通过标准 |
|--------|----------|----------|
| 分类一致性 | `template.ts` vs `templateSchema.ts` vs 磁盘模板 | 10 个分类，三方完全一致 |
| 字段完整性 | 遍历 `templates/*.json` | 48/48 模板字段完整，无缺失 |
| 命名唯一性 | 遍历 `templates/*.json` | 48/48 template_id 和 title 全局唯一 |
| ID-文件名一致性 | 遍历 `templates/*.json` | template_id 与文件名一一对应 |
| 旧模板迁移质量 | 抽查 dudou/siuf 模板 | prompt_blocks 8 字段齐全，语义合理 |

## 三、前端功能验收（手动测试）

| 验收项 | 操作步骤 | 通过标准 |
|--------|----------|----------|
| 模板列表加载 | 打开 `http://localhost:3000` | 左侧显示 48 个模板卡片 |
| 分类筛选 | 点击分类标签（如"人像写真"） | 仅显示该分类模板，数量正确 |
| 模板详情 | 点击模板卡片 | 右侧显示 category / title / ratio / style / face_lock / scene / clothing |
| 生成写真 | 点击"生成写真"按钮 | 结果区显示"生成中"→任务状态 + prompt 预览 |
| 任务信息 | 查看结果区 | 显示 task_id / status / template 信息 |
| mock 提示 | 查看图片占位区 | mock 模式下显示友好提示（非空白） |

## 四、Python 后端验收

| 验收项 | 命令 | 通过标准 |
|--------|------|----------|
| FastAPI 启动 | `uv run uvicorn backend.main:app --reload` | `localhost:8000` 可访问，`/health` 返回 `ok` |
| SenseNova SDK | `python /tmp/sense_test.py` | 认证通过（不再报 `No secret access key`） |

## 五、工程纪律遵守情况

| 纪律 | 要求 | 实际 | 状态 |
|------|:----:|:----:|:----:|
| 先输出补丁计划 | 改前必须先输出计划 | 本轮超发前未输出 | ❌ 下轮严格执行 |
| 最多改 3 个文件 | 每轮 ≤ 3 个文件 | 实际修改 19+ 个 | ❌ 已反思 |
| 不修改 .env | .env 零改动 | 未修改 | ✅ |
| 不上传 COS | 零云端上传 | 未上传 | ✅ |
| 不接入真 API | 维持 mock | 未接入 | ✅ |
| 不重装依赖 | package.json / requirements.txt 不动 | 未重装 | ✅ |
| 不删除文件 | 原有文件全部保留 | 未删除原有文件 | ✅ |
| 不格式化全项目 | 仅改动的文件做最小改动 | 未全项目格式化 | ✅ |
| 模板扩充节奏 | 每类 3 个 / 总计 9 个 / 轮 | 实际 11 个 | ❌ 已反思 |

## 六、本轮验收结果

| 维度 | 状态 |
|------|:----:|
| 工程底座 | ✅ 通过 |
| 数据质量 | ✅ 通过 |
| 前端功能 | ✅ 通过（mock 模式） |
| Python 后端 | ⚠️ 认证修复通过，真 API 未验证 |
| 工程纪律 | ⚠️ 2 项超发（文件数、模板数），已记录反思 |

**综合判定：本轮验收通过，但纪律执行有偏差，下轮严格执行补丁计划流程。**
